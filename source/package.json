{
  "name": "awstats-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "awstats generate",
    "minify": "awstats minify",
    "server": "awstats server",
    "deploy": "awstats deploy",
    "clean": "awstats clean"
  },
  "awstats": {
    "version": ""
  },
  "dependencies": {
    "awstats": "^1.0.0"
  }
}
